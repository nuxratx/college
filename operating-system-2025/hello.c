/* 
C requires the source code to be completely compiled before execution. 
C requires all variables to be declared before use
C is statically typed
In C whitesoace does not matter unlike python where it does matter. 
In Python you used the print method
In C we have a function named printf
Both allow escape characters
In C the escape characters are part of the syntax
Replacement characters 
%d = integer
%f = double in decimal form
*/

#include <stdio.h>
int main() {
   // printf() displays the string inside quotation
   printf("Hello World");
   return 0;
}
